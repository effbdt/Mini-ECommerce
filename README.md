Mini E-Commerce project

This is a frontend only application built with Vue.js 3
The project contains everything neccessary, it properly uses Vue specific features, with a clear structure.


Running the project:
1. Clone the repository

    git clone https://github.com/effbdt/Mini-ECommerce

2. Install the dependencies

    npm install

3. Run the development server

    npm run dev
